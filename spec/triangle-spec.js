/* eslint-disable no-unused-vars */
import { Date } from "../src/triangletracker";

describe('Date',function(){
    it('should test to see if its a leap year', function(){
    var date = new Date(03,20,2019);
    // expect(leapYear(date).toEqual("false");

    });

});
/* eslint-disable no-unused-vars */
